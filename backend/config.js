const {
    PORT,
    PROJECT_NAME,
    API_VERSION,
    NODE_ENV,
    MONGO_URL,
    SESS_NAME,
    SESS_SECRET,
    SESS_LIFETIME
} = process.env

module.exports = {
    PORT,
    PROJECT_NAME,
    API_VERSION,
    NODE_ENV,
    MONGO_URL,
    SESS_NAME,
    SESS_SECRET,
    SESS_LIFETIME
}